# 重构架构说明书

> 项目：cs-markdown-editor（Flet 0.86.2 + Python 3.12 的 Typora 式 WYSIWYG Markdown 编辑器）
> 目标：在保留全部功能逻辑的前提下，按单一职责原则拆分模块、消除冗余、统一命名，
>      将两大「巨石组件」（原 `main.py` 1632 行 50+ 闭包、原 `views/editor.py` 3027 行 80+ 闭包）
>      拆分为 **AppContext / EditorContext + 控制器 / 工厂** 模式的包结构，让人工维护更清晰。

---

## 一、拆分后的目录结构

```
cs-markdown-editor/
├── main.py                  # 入口：注册字体/主题、page.render(App)（精简，不含 App 逻辑）
├── parser.py                # Markdown ↔ Document 双向转换（行级 + 段级解析）
├── styles.py                # 主题配色、段→TextStyle 映射、Border 工具
├── settings.json            # 用户设置持久化（运行期生成）
├── pyproject.toml
│
├── assets/                  # 字体、图标等静态资源
│
├── config/                  # 配置层：应用级常量、默认设置、示例文档
│   ├── __init__.py
│   ├── settings.py          # DEFAULT_SETTINGS / load_settings / save_settings（深合并）
│   └── sample.py            # SAMPLE_MD（启动示例文档）
│
├── models/                  # 数据结构层：三级状态模型（文档/行/段）
│   ├── __init__.py          # 重新导出 SegType / BlockType / Segment / Line / Document
│   └── document.py          # @ft.observable 数据类定义
│
├── core/                    # 核心流程层：编辑器状态容器与对外动作集合
│   ├── __init__.py          # 重新导出 CursorState / EditorActions / EditHistory
│   ├── cursor.py            # CursorState（光标位置 ref 镜像）
│   ├── actions.py           # EditorActions（编辑器对外动作集合 dataclass + PEP 695 类型别名）
│   └── history.py           # EditorSnapshot / EditHistory（撤销/重做栈，混合快照）
│
├── utils/                   # 通用工具层：无项目内依赖的纯函数与常量
│   ├── __init__.py          # 重新导出 4 个工具子模块
│   ├── file_helpers.py      # file_name(path)
│   ├── segment_helpers.py   # PREFIX_SEGTYPES / WRAP_SYNTAX / display_text / split_seg_for_display / is_fence / line_raw
│   ├── table_helpers.py     # ALIGN_RE / split_row / join_row / is_table_separator
│   └── text_layout.py       # measure_text_width / image_fit_size / FONT_FILES
│
├── services/                # 业务逻辑层：快捷键管理、文件 IO
│   ├── __init__.py          # 重新导出 file_io / shortcuts
│   ├── shortcuts.py         # ShortcutManager + DEFAULT_SHORTCUTS
│   └── file_io.py           # read_text / write_text
│
├── app/                     # App 组件包（AppContext + 控制器模式，原 main.py 拆分）
│   ├── __init__.py          # App 组件：hooks → ctx 构造 → 控制器装配 → use_effect → render
│   ├── _context.py          # AppContext dataclass（状态容器：稳定区+快照区+装配槽）
│   ├── _tab_management.py   # 标签 CRUD / 切换 / 批量关闭 / 关闭确认
│   ├── _tab_helpers.py      # 纯函数（is_blank_untitled / tab_is_dirty 等）
│   ├── _file_io_ops.py      # 文件读写/打开/保存/导出
│   ├── _file_dialogs.py     # 文件对话框 + 右键菜单分发
│   ├── _diff_controller.py  # 对比标签创建 / 脏状态管理
│   ├── _settings_controller.py  # 设置 / 主题 / 快捷键捕获 / 侧边栏
│   ├── _split_editor.py     # 拆分编辑器 / 对比焦点视口切换
│   ├── _focus_router.py     # 焦点路由 / 跳转 / 脏状态上报
│   ├── _keyboard.py         # KeyDispatcher 装配 + 键盘绑定
│   ├── _render.py           # 渲染树：sidebar / editor_area / footer / tab_bar / dialogs / Stack
│   ├── autosave.py          # Autosave（debounce 2s 异步回写，类封装）
│   └── diff_scroll_sync.py  # DiffScrollSync（对比双视口像素级同步滚动，类封装）
│
└── views/                   # 视图层：声明式 Flet 组件
    ├── __init__.py
    ├── _editor_helpers.py   # 编辑器纯函数（步进/词边界/删除决策/视觉行 X→off）跨组共享
    ├── editor/              # 编辑器根组件包（EditorContext + 工厂模式，原 editor.py 拆分）
    │   ├── __init__.py      # MarkdownEditor：hooks → ctx → 工厂调用 → 装配 → 渲染
    │   ├── _context.py      # EditorContext dataclass（双区 + 装配槽）
    │   ├── _cursor.py       # 光标 / IME 输入核心组（紧耦合不拆散）
    │   ├── _navigation.py   # 光标移动（视觉行 / 垂直导航 / 记忆列）
    │   ├── _scroll.py       # 滚动 / 行高缓存 / 命中测试 / 跳转
    │   ├── _outward.py      # 向外选区（步进 / 扩展 / 删除 / 剪切 / 复制）
    │   ├── _clipboard.py    # 剪贴板 / SelectionArea 选区
    │   ├── _fence.py        # 围栏岛屿（CODE / MATH / TABLE）编辑
    │   ├── _render.py       # 行控件列表构造（LineView / TableView / diff 着色）
    │   ├── _history.py      # 撤销/重做（行级快照 + 全文快照混合）
    │   ├── _indent.py       # 列表 / 引用缩进
    │   ├── _blocks.py       # 块类型切换 / 行合并 / 换行提交
    │   ├── _inline_format.py# 行内格式快捷键（加粗/斜体/代码/链接…）
    │   ├── _raw_mode.py     # 原文模式切换
    │   ├── _focus.py        # 焦点 / 跳转高亮脉冲
    │   ├── _key.py          # KeyDispatcher 装配（编辑器层键盘分发）
    │   ├── _actions.py      # EditorActions 装配（上抛给 App 层）
    │   └── _helpers.py      # 编辑器内部共享小工具
    ├── line_view.py         # 行视图（围栏岛屿 / RenderedLine + Stack）
    ├── rendered_line.py     # 渲染层：TextSpan + GestureDetector 命中
    ├── cursor_layer.py      # 透明光标 TextField（IME 友好）
    ├── pixel_layout.py      # 像素布局缓存 + 命中测试
    ├── segment_view.py      # 段级 TextSpan 渲染
    ├── key_bindings.py      # KeyDispatcher（App 层键盘事件分发）
    ├── toolbar.py           # 顶部工具栏
    ├── sidebar.py           # 侧边栏（文件树/大纲/历史）
    ├── tab_bar.py           # 多标签栏（含右键菜单 + 关闭确认）
    ├── status_bar.py        # 底部状态栏
    ├── settings_dialog.py   # 设置弹层（编辑/外观/行为/快捷键/高级）
    ├── table_view.py        # 表格编辑组件（DataTable2 独立岛屿）
    ├── diff_view.py         # 文件对比双编辑器视图
    ├── raw_editor.py        # 原文模式编辑器
    ├── tool_area.py         # 工具区容器
    └── file_dialogs.py      # 文件对话框原生封装
```

---

## 二、每个模块的职责与对外接口

### 1. `models/` — 数据结构层

| 文件 | 职责 | 对外接口 |
|------|------|---------|
| `document.py` | 定义文档三级状态模型，全部 `@ft.observable`，字段变更自动触发依赖组件重绘 | `SegType` / `BlockType`（StrEnum）、`Segment` / `Line` / `Document`（dataclass） |

**依赖项**：标准库 `dataclasses` / `enum`、`flet`。
**边界**：不依赖项目内任何模块，是整个依赖图的根。

### 2. `utils/` — 通用工具层

| 文件 | 职责 | 对外接口 |
|------|------|---------|
| `file_helpers.py` | 文件名派生 | `file_name(path: str \| None) -> str` |
| `segment_helpers.py` | 段类型常量、显示文本、包裹语法、段拆分、围栏判定、行 raw 取值（消除 parser/views 重复） | `PREFIX_SEGTYPES` / `MONO_SEGTYPES` / `WRAP_SYNTAX` / `display_text(seg)` / `split_seg_for_display(seg)` / `is_fence(line)` / `line_raw(line)` |
| `table_helpers.py` | 表格行解析与拼接（消除 parser/editor/table_view 重复） | `ALIGN_RE` / `split_row(raw)` / `join_row(cells)` / `is_table_separator(raw)` |
| `text_layout.py` | 文本像素测量与图片尺寸（从 styles.py 拆出） | `measure_text_width(text, font_family, size)` / `image_fit_size(src, max_size=500)` / `FONT_FILES` |

**依赖项**：`models`、标准库、`Pillow`。
**边界**：仅被 `parser` / `views` / `styles` 单向引用，自身不引用项目内其它层。

### 3. `core/` — 核心流程层

| 文件 | 职责 | 对外接口 |
|------|------|---------|
| `cursor.py` | TextField 光标位置镜像（用 `ft.use_ref` 而非 state，避免重渲染跳光标） | `CursorState`（base / extent / draft_len + `reset(pos, draft_len)`） |
| `actions.py` | 编辑器对外动作集合 dataclass（替代 nav_ref.current 大字典）+ PEP 695 类型别名 | `EditorActions`（50+ 字段：cursor_li / cursor_off / move_left / handle_paste / undo / ...）、`OutwardSel` / `ScrollState` 类型别名 |
| `history.py` | 撤销/重做栈（从 services/history.py 迁入） | `EditorSnapshot`（frozen dataclass）、`EditHistory`（push / pop_undo / pop_redo） |

**依赖项**：`models`、`flet`、标准库。
**边界**：编辑器核心状态管理三件套同层，互相可引用；不被 `utils` / `services` 反向引用。

### 4. `services/` — 业务逻辑层

| 文件 | 职责 | 对外接口 |
|------|------|---------|
| `shortcuts.py` | 快捷键管理：动作注册、键位匹配、冲突检测（从 main.py 抽出 ~200 行） | `DEFAULT_SHORTCUTS`、`ShortcutManager` 类 |
| `file_io.py` | UTF-8 文件读写（统一异常处理策略） | `read_text(path)` / `write_text(path, text)` |

**依赖项**：`flet`、标准库。
**边界**：`shortcuts` 不依赖 `config`（避免循环），`config.settings` 反向依赖 `services.shortcuts.DEFAULT_SHORTCUTS` 做深合并。

### 5. `config/` — 配置层

| 文件 | 职责 | 对外接口 |
|------|------|---------|
| `settings.py` | 应用级默认设置、加载、保存、深合并 | `DEFAULT_SETTINGS` / `SETTINGS_PATH` / `load_settings()` / `save_settings(settings)` |
| `sample.py` | 启动示例 Markdown 文档 | `SAMPLE_MD` |

**依赖项**：`services.shortcuts.DEFAULT_SHORTCUTS`、标准库 `json` / `os`。
**边界**：仅提供常量与读写函数，不含业务逻辑。

### 6. `app/` — App 组件包（原 main.py 拆分）

采用 **AppContext + 控制器** 模式：`__init__.py` 内 `App()` 组件按「hooks → ctx 构造 → 控制器装配 → use_effect → render」顺序组织，所有闭包组按职责拆到独立模块，通过 `AppContext` 装配槽串联。

| 文件 | 职责 | 对外接口 |
|------|------|---------|
| `__init__.py` | App 组件：声明 hooks（state/refs）→ 构造 AppContext → 调各 `build_*` 装配控制器 → use_effect 绑定副作用 → `build_render(ctx)` | `App` 组件 |
| `_context.py` | AppContext dataclass：集中所有 state 值（快照区）/ setters+refs（稳定区）/ 装配槽（跨控制器调用） | `AppContext` |
| `_tab_management.py` | 标签 CRUD / 切换 / 批量关闭 / 关闭确认（cur_tab / update_active / select_tab / close_tab / save_and_close_pending …） | `build_tab_management(ctx) -> dict` |
| `_tab_helpers.py` | 纯函数（无闭包依赖，可单测）：is_blank_untitled / tab_is_dirty | `is_blank_untitled` / `tab_is_dirty` |
| `_file_io_ops.py` | 文件读写/打开/保存/导出（open_file / save_doc / export_html …） | `build_file_io_ops(ctx) -> dict` |
| `_file_dialogs.py` | 文件对话框 + 右键菜单分发 | `build_file_dialogs(ctx) -> dict` |
| `_diff_controller.py` | 对比标签创建 / 脏状态管理 | `build_diff_controller(ctx) -> dict` |
| `_settings_controller.py` | 设置 / 主题 / 快捷键捕获 / 侧边栏 | `build_settings_controller(ctx) -> dict` |
| `_split_editor.py` | 拆分编辑器 / 对比焦点视口切换 | `build_split_editor(ctx) -> dict` |
| `_focus_router.py` | 焦点路由 / 跳转 / 脏状态上报 | `build_focus_router(ctx) -> dict` |
| `_keyboard.py` | KeyDispatcher 装配 + 键盘绑定（浏览态层） | `build_keyboard(ctx) -> dict` |
| `_render.py` | 渲染树：sidebar / editor_area / footer / tab_bar / dialogs / Stack | `build_render(ctx) -> ft.Control` |
| `autosave.py` | 自动保存（debounce 2s 异步回写，类封装管理副作用） | `Autosave` 类 |
| `diff_scroll_sync.py` | 对比双视口像素级同步滚动（类封装状态机：一侧到底另一侧可继续） | `DiffScrollSync` 类 |

**依赖项**：`models` / `core` / `utils` / `services` / `config` / `parser` / `styles` / `views`。
**边界**：控制器模块只读 ctx、写 state；跨控制器调用通过 ctx 装配槽（`cur_tab_fn` / `update_active` …）解耦，禁止直接 import 互相调用。

### 7. `views/editor/` — 编辑器根组件包（原 views/editor.py 拆分）

采用 **EditorContext + 工厂** 模式：`__init__.py` 内 `MarkdownEditor()` 组件按「hooks → ctx 构造 → 工厂调用 → 装配 → 渲染」顺序组织，80+ 闭包按职责拆到 17 个工厂模块，通过 `EditorContext` 装配槽串联。

| 文件 | 职责 | 对外接口 |
|------|------|---------|
| `__init__.py` | MarkdownEditor 组件：hooks → ctx → 工厂调用 → 装配 → 渲染（含 TOC 增量哈希优化） | `MarkdownEditor` 组件 |
| `_context.py` | EditorContext dataclass：双区（快照区+稳定区）+ 装配槽 | `EditorContext` |
| `_cursor.py` | 光标 / IME 输入核心组（紧耦合不拆散：set_cursor / handle_char_input / clear_value …） | `build_cursor(ctx) -> dict` |
| `_navigation.py` | 光标移动（视觉行 / 垂直导航 / 记忆列 / Home/End/PageUp/PageDown） | `build_navigation(ctx) -> dict` |
| `_scroll.py` | 滚动 / 行高缓存 / 命中测试 / 跳转 | `build_scroll(ctx) -> dict` |
| `_outward.py` | 向外选区（步进 / 扩展 / 删除 / 剪切 / 复制 / 选词） | `build_outward(ctx) -> dict` |
| `_clipboard.py` | 剪贴板 / SelectionArea 选区（粘贴拆行 / 剪切竞态处理） | `build_clipboard(ctx) -> dict` |
| `_fence.py` | 围栏岛屿（CODE / MATH / TABLE）编辑 | `build_fence(ctx) -> dict` |
| `_render.py` | 行控件列表构造（LineView / TableView / diff 着色） | `build_render(ctx) -> ft.Control` |
| `_history.py` | 撤销/重做（行级快照 + 全文快照混合） | `build_history(ctx) -> dict` |
| `_indent.py` | 列表 / 引用缩进（Tab / Shift+Tab） | `build_indent(ctx) -> dict` |
| `_blocks.py` | 块类型切换 / 行合并 / 换行提交 | `build_blocks(ctx) -> dict` |
| `_inline_format.py` | 行内格式快捷键（加粗/斜体/代码/链接…，Toggle 行为） | `build_inline_format(ctx) -> dict` |
| `_raw_mode.py` | 原文模式切换 | `build_raw_mode(ctx) -> dict` |
| `_focus.py` | 焦点 / 跳转高亮脉冲 | `build_focus(ctx) -> dict` |
| `_key.py` | KeyDispatcher 装配（编辑器层键盘分发） | `build_key(ctx) -> dict` |
| `_actions.py` | EditorActions 装配（上抛给 App 层） | `build_actions(ctx) -> EditorActions` |
| `_helpers.py` | 编辑器内部共享小工具 | 内部函数 |

**依赖项**：`models` / `core` / `utils` / `services` / `config` / `parser` / `styles` / 同层 `views.*`。
**边界**：工厂模块只读 ctx、通过 setters 写 state；同组闭包直接互调，跨组通过 ctx 装配槽；纯计算抽取到 `views/_editor_helpers.py` 可单测。

### 8. `views/`（顶层）— 其他视图组件

| 文件 | 职责 | 对外接口 |
|------|------|---------|
| `_editor_helpers.py` | 编辑器纯函数（步进/词边界/删除决策/视觉行 X→off）跨工厂共享、可单测 | `_step_left` / `_step_right` / `_select_word_bounds` / `_compute_delete_result` / `_vline_off_at_x` |
| `line_view.py` | 行外层包装：缩进/引用边框、激活态切换、向外选区协调 | `LineView` 组件 |
| `rendered_line.py` | 渲染层行组件：`raw_to_visible_spans` + GestureDetector 命中测试 | `RenderedLine` 组件 |
| `cursor_layer.py` | 透明光标 TextField（编辑层，捕获键盘输入即时渲染） | 内部组件 |
| `pixel_layout.py` | 行内 X 偏移测量与命中测试（中点吸附 + 折叠标记扫描） | `_line_raw_offsets_x` / `hit_test_line_x_raw` |
| `segment_view.py` | 段→TextSpan 渲染：渲染态透明、编辑态灰色 | `segment_to_span` / `raw_to_visible_spans` / `selection_highlight_bg` |
| `key_bindings.py` | App 层键盘事件分发器：浏览态/编辑态两层快捷键 + 光标导航 | `KeyDispatcher` |
| `toolbar.py` / `sidebar.py` / `tab_bar.py` / `status_bar.py` | 工具栏 / 侧边栏 / 标签栏 / 状态栏 | 对应组件 |
| `settings_dialog.py` | 设置弹层（编辑/外观/行为/快捷键/高级五页） | `SettingsDialog` |
| `table_view.py` | 表格编辑组件（DataTable2 独立岛屿） | `TableView` |
| `diff_view.py` | 文件对比双编辑器视图 | `DiffView` |
| `raw_editor.py` / `tool_area.py` / `file_dialogs.py` | 原文编辑器 / 工具区容器 / 文件对话框 | 对应组件 |

**依赖项**：`models` / `core` / `utils` / `services` / `config` / `parser` / `styles`。
**边界**：视图层只读状态、不持久化；所有状态变更通过 `core.EditorActions` 上抛给 `app/`。

### 9. 顶层模块

| 文件 | 职责 |
|------|------|
| `main.py` | 应用入口：注册字体/主题、`page.render(App)`（精简，不再含 App 组件逻辑） |
| `parser.py` | Markdown 双向转换：`parse_markdown` / `reparse_line_atomic` / `staging_reparse` / `serialize` / `to_html` / 选区操作 |
| `styles.py` | 主题配色（亮/暗）、块级排版、段样式映射、Border 工具 |

---

## 三、模块依赖关系图

```
                          ┌──────────┐
                          │ main.py  │  入口：注册字体/主题、page.render(App)
                          └────┬─────┘
                               ▼
                          ┌──────────┐
                          │  app/    │  App 组件包（AppContext + 控制器模式）
                          └────┬─────┘
              ┌────────────────┼────────────────┐
              ▼                ▼                ▼
        ┌──────────┐    ┌──────────┐     ┌──────────┐
        │  views/  │    │ parser.py│     │ styles.py│
        │ (editor/ │    └────┬─────┘     └────┬─────┘
        │  +顶层)  │         │                │
        └────┬─────┘         │                │
   ┌─────────┼─────────┐    │             ┌───┘
   ▼         ▼         ▼    ▼             ▼
┌──────┐ ┌──────┐ ┌────────┴──┐ ┌──────────────┐
│ core │ │utils │ │ services  │ │   config     │
└──┬───┘ └──┬───┘ └─────┬─────┘ └──────┬───────┘
   │        │           │              │
   │        │           │     ┌────────┘
   │        │           │     │  (config.settings
   │        │           │     │   依赖 services.shortcuts
   │        │           │     │   做深合并，单向)
   │        │           │     │
   └────────┴───────────┴─────┘
              ▼
         ┌─────────┐
         │ models  │  依赖根（仅依赖 flet + 标准库）
         └─────────┘
```

### 依赖方向规则（强制单向）

| 层 | 可依赖 |
|----|--------|
| `models` | 仅 `flet` + 标准库 |
| `utils` | `models` + 标准库 + `Pillow` |
| `core` | `models` + `flet` + 标准库 |
| `services` | `flet` + 标准库（`shortcuts` 不依赖 `config`） |
| `config` | `services.shortcuts` + 标准库 |
| `parser` | `models` + `utils` + `mistune` |
| `styles` | `models` + `flet` |
| `views` | `models` / `core` / `utils` / `services` / `config` / `parser` / `styles` |
| `app` | 所有层（含 `views`） |
| `main` | `app` |

**循环依赖检查**：通过 `grep` 全项目 `from/import` 语句验证，无循环依赖。
唯一需要留意的反向引用是 `config.settings` → `services.shortcuts.DEFAULT_SHORTCUTS`，
但 `services.shortcuts` 不反向引用 `config`，故仍为单向 DAG。

---

## 四、关键设计决策

### 4.1 状态管理：`core/` 三件套同层

`cursor.py` / `actions.py` / `history.py` 共同构成编辑器核心状态管理。
原架构把它们分散在 `state/`（actions、cursor）与 `services/`（history）两个目录，
导致 `services/` 既有 IO 工具又有编辑器状态，职责混乱。
现统一到 `core/`，对外通过 `from core import CursorState, EditorActions, EditHistory` 一行导入。

### 4.2 `EditorActions` dataclass 替代字典 nav_ref

原 `main.py` 通过 `nav_ref.current = {"move_left": fn1, "move_right": fn2, ...}` 大字典
传递 20+ 编辑器动作给键盘分发器，存在三个维护痛点：

- 缺键时 `nav.get("xxx")` 静默返回 `None`，运行期才暴露
- 字符串 key 无类型提示，IDE 无法跳转
- 字段含义靠注释维护，易过期

重构为 `@dataclass(slots=True) EditorActions`：

- 必填字段构造时即校验，缺失立即 `TypeError`
- 字段名即文档，IDE 自动补全 + 跳转
- 字段类型注解明确（`Callable[[], None]` / `ft.Ref` / `int | None`）
- PEP 695 `type` 语句定义 `OutwardSel` / `ScrollState` 类型别名，替代裸 tuple 注解

### 4.3 `utils/` 提取跨模块重复

| 重复片段 | 原位置 | 提取到 |
|---------|--------|--------|
| `_PREFIX_SEGTYPES` 常量 | parser / segment_view / rendered_line | `utils.segment_helpers.PREFIX_SEGTYPES` |
| `_WRAP_SYNTAX` / `_WRAP_MAP` / `_WRAP_CHAR` | parser / editor / segment_view | `utils.segment_helpers.WRAP_SYNTAX` |
| `_display_text` / `_seg_display_text` | parser / segment_view | `utils.segment_helpers.display_text` |
| `_split_seg_for_display` | segment_view | `utils.segment_helpers.split_seg_for_display` |
| `_is_fence` / `_line_raw` | editor / outward | `utils.segment_helpers.is_fence` / `line_raw` |
| `_split_row` / `_join_row` / `_ALIGN_RE` | parser / editor / table_view | `utils.table_helpers` |
| `_is_table_separator` | parser / table_view | `utils.table_helpers.is_table_separator` |
| `_file_name` | main / editor / status_bar / tab_bar | `utils.file_helpers.file_name` |
| `measure_text_width` / `image_fit_size` | styles.py（混合职责） | `utils.text_layout` |

### 4.4 `styles.py` 职责瘦身

原 `styles.py` 混合四个职责：主题颜色、排版样式、文本测量、图片尺寸。
重构后：

- `styles.py` 仅保留主题颜色 + 段→TextStyle 映射 + Border 工具
- 文本测量与图片尺寸独立到 `utils/text_layout.py`
- `utils/text_layout.py` 自带 `FONT_MAIN` / `FONT_MONO` 常量副本，避免反向依赖 `styles`

### 4.5 `views/key_bindings.py` 焦点守卫统一

原代码块与表格的「原生控件聚焦时跳过全局快捷键」逻辑各写一份，散落在 `KeyDispatcher.handle`。
重构为 `_native_field_focused(actions)` 静态方法 + `_NATIVE_NAV_KEYS` / `_NATIVE_CLIPBOARD_COMBO`
两个 `frozenset` 常量，单一来源、易于扩展（新增原生编辑控件只需改一处）。

### 4.6 巨石组件拆分：Context + 控制器/工厂模式

原 `main.py`（1632 行 50+ 闭包）与 `views/editor.py`（3027 行 80+ 闭包）是架构债务核心。
Flet 的 hooks 必须在组件体内顺序调用，无法直接把闭包移到独立文件按传统类拆分，
故采用 **Context + 控制器/工厂** 模式：

```
App()/MarkdownEditor() 组件体内：
  1. hooks 区        —— use_state / use_ref / use_memo 顺序调用（Flet 强制顺序）
  2. ctx 构造        —— AppContext / EditorContext dataclass 装配所有 state/refs/setters/装配槽
  3. 控制器/工厂装配 —— build_xxx(ctx) 返回 dict[str, Callable]，按拓扑序装配到 ctx 装配槽
  4. use_effect 区   —— 绑定副作用（focus / theme / keyboard）
  5. render          —— build_render(ctx) 返回 ft.Control
```

**AppContext / EditorContext**（`@dataclass(kw_only=True)`）分三区：

- **快照区**：state 值（tabs / cursor_li / settings …），每次渲染重建
- **稳定区**：setters + refs（set_tabs / cursor_ref …），跨渲染稳定引用
- **装配槽**：跨控制器/工厂调用（`cur_tab_fn` / `update_active` …），`field(default=lambda: None)` 兜底

**控制器/工厂模块**（`build_xxx(ctx) -> dict`）：

- 只读 ctx、通过 setters 写 state
- 同组闭包直接互调，跨组通过 ctx 装配槽（解耦）
- 纯计算抽取到 `_helpers.py` / `_editor_helpers.py` 可单测

### 4.7 纯函数抽取与可测试性

巨石组件拆分时，将状态无关的纯计算从闭包抽取到独立模块，使其可单测：

| 纯函数 | 抽取到 | 用途 |
|--------|--------|------|
| `_step_left` / `_step_right` | `views/_editor_helpers.py` | 段间步进（接收 lines 参数） |
| `_select_word_bounds` | `views/_editor_helpers.py` | 双击选词边界（VSCode 风格） |
| `_compute_delete_result` | `views/_editor_helpers.py` | 删除/合并决策（行级） |
| `_vline_off_at_x` | `views/_editor_helpers.py` | 视觉行 X→raw off |
| `is_blank_untitled` / `tab_is_dirty` | `app/_tab_helpers.py` | 标签状态判定 |

配套新增 `tests/test_tab_management.py`、`tests/test_editor_helpers.py` 等单测覆盖纯函数与控制器逻辑，
在无 UI 回归测试的前提下保证拆分行为一致。

### 4.8 状态机类封装

将复杂状态转移与副作用封装为类，替代散落的闭包 + ref：

| 类 | 位置 | 职责 |
|----|------|------|
| `Autosave` | `app/autosave.py` | debounce 2s 异步回写，管理定时器与脏状态 |
| `DiffScrollSync` | `app/diff_scroll_sync.py` | 对比双视口像素级同步滚动状态机（一侧到底另一侧可继续） |

### 4.9 模块文档规范

每个 `.py` 文件顶部统一格式：

```python
"""<模块一句话职责>。

依赖项：
- <包/模块>

对外接口：
- <符号>：<说明>

设计要点 / 冗余治理：
- <关键决策>
"""
```

便于维护者一眼看清模块边界与变更影响面。

---

## 五、命名规范统一

| 旧名（模糊/歧义） | 新名 | 理由 |
|------------------|------|------|
| `nav_ref.current = {...}` | `EditorActions` 实例 | 字典→dataclass，类型安全 |
| `active` / `active_seg` / `draft` | `cursor_li` / `cursor_off` | 三状态合并为光标位置单一来源 |
| `_PREFIX_SEGTYPES` 等前缀下划线 | `PREFIX_SEGTYPES`（公开） | 跨模块共享，不应私有 |
| `_display_text` / `_seg_display_text` | `display_text` | 统一为一个名字 |
| `state.actions` / `state.cursor` | `core.actions` / `core.cursor` | 目录名与职责对齐 |
| `services.history` | `core.history` | 编辑器状态归 core，非业务 IO |
| `nav["move_left"]()` | `actions.move_left()` | 属性访问替代字符串下标 |
| `main.py` App 逻辑 | `app/` 包 + `AppContext` | 巨石拆分，职责单一 |
| `views/editor.py` 80+ 闭包 | `views/editor/` 包 + `EditorContext` | 巨石拆分，职责单一 |

---

## 六、Python 3.12 + Flet 0.86.2 最佳实践落地

| 特性 | 应用位置 |
|------|---------|
| `StrEnum`（3.11+） | `models.SegType` / `BlockType`：兼顾类型安全与字符串兼容 |
| PEP 695 `type` 语句（3.12） | `core/actions.py`：`type OutwardSel = tuple[int, int, int, int] \| None`、`type ScrollState = tuple[float, float, float]` |
| `@dataclass(kw_only=True)`（3.10+） | `AppContext` / `EditorContext`：强制关键字传参，避免位置参数错位 |
| `@dataclass(slots=True)`（3.10+） | `EditorActions`：__slots__ 减少内存占用、加速属性访问 |
| `@dataclass(frozen=True)` | `core.EditorSnapshot`：撤销栈不可变快照 |
| `@dataclass` + `field(default_factory=...)` | `models.Line.segments` / `Document.lines`：避免可变默认值陷阱 |
| `X \| None` 联合类型 | 全项目类型注解（替代 `Optional[X]`） |
| `tuple[X, ...]` / `list[X]` 泛型 | `Segment.marks: tuple` 等 |
| `collections.abc.Callable[[], None]` | `EditorActions` / `AppContext` / `EditorContext` 字段类型 |
| `@ft.observable` | `Segment` / `Line` / `Document`：字段变更自动触发依赖组件重绘 |
| `@ft.component` | `app.App` / `views.editor.MarkdownEditor` / 各视图组件：声明式渲染 |
| `ft.use_state` / `ft.use_ref` / `ft.use_effect` | 函数式组件状态管理 |
| `ft.use_memo` | `ShortcutManager` 缓存（`[id(settings)]` 依赖）、TOC 增量哈希（`tuple` 签名避免大字符串拼接） |
| `frozenset` | `KeyDispatcher._NATIVE_NAV_KEYS`：不可变常量集合，查找 O(1) |

### 性能优化要点

| 优化 | 位置 | 说明 |
|------|------|------|
| TOC 增量哈希 | `views/editor/__init__.py` | `tuple((i, level, raw) ...)` 签名替代字符串拼接；raw 同对象 O(1) 比较 |
| ShortcutManager 缓存 | `app/__init__.py` | `use_memo(lambda: ShortcutManager(...), [id(settings)])` 避免频繁文档编辑时重复解析 |
| 行级撤销快照 | `core/history.py` | `LineEditSnapshot` 单行编辑 O(1)/操作，全文快照仅在结构变化时入栈；栈容量 50 防膨胀 |
| 原子化重解析 | `parser.reparse_line_atomic` | 高频编辑路径仅触发 1 次 observable 通知 |
| 透明 TextField 不设 value | `views/cursor_layer.py` | IME 友好，value 由 use_effect 异步清空，同行输入 key 不变保组合态 |

---

## 七、验证

- ✅ 全项目 79 个 `.py` 文件（不含 tests）通过 `ast.parse` 语法检查
- ✅ `tests/` 21 个测试文件，覆盖 parser / utils / core / app 控制器 / views 纯函数层
- ✅ `from/import` 全量扫描确认无循环依赖
- ✅ 旧目录 `state/`、根 `models.py`、`services/history.py`、巨石 `views/editor.py` 已拆分为包
- ✅ 所有 `__init__.py` 重新导出公共符号，保持 `from models import Document` 等引用兼容
- ✅ Ruff（I001 导入排序 / B015 无效比较等）全部修复
- ✅ AppContext / EditorContext + 控制器/工厂模式落地，原 1632+3027 行两巨石拆为 14+17 个职责单一模块
