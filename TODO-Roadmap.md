# ﻿Roadmap

> 跨平台、替代 Typora、VSCode（markdown 部分）的文档编辑器/写作工具/知识管理工具/个人操作系统
- 特性：
  - 完全本地化：支持本地文件（类似 typora 编辑）、本地文件夹（类似 obsidian 仓库）
  - 所见即所得：基于 Markdown 语法
  - 多层级文件目录组织 + 标签体系管理：多级标签
  - 实时保存，历史版本
  - 导出功能有：PDF、Docx
  - 全局搜索
- 界面布局：
  - 功能区：最左侧图标栏，文件、搜索、图谱等快捷入口
  - 左侧边栏：文件树、标签、图谱等二级入口
  - 右侧边栏：大纲
  - 标签栏目：多个标签的分栏
  - 编辑区：显示与编辑
  - 底部状态栏：字数、编辑器状态等
- 暂不考虑：
  - 双链
  - 标签体系：多级标签
  - 数据库：每个文件都有属性
  - 书签
  - 模板
  - 加密笔记
- 功能特性：

# TODO
```ini
为现有文档编辑器完整集成Git版本管理模块，严格对标VS Code内置Git功能
# 核心要求：功能无遗漏、交互符合用户直觉、不侵入原有编辑能力

## 一、更改文件管理（查看更改文件）
1. 侧边栏功能区独立Git面板，分区展示「工作区更改」「暂存区更改」，文件标记Git原生状态：新增/修改/删除/重命名
2. 点击文件唤起内嵌diff视图：行级增删高亮，支持统一/分栏视图切换，与编辑器行号联动定位
3. 操作集：单文件暂存/取消暂存、批量暂存全部、单文件丢弃更改、批量丢弃所有工作区更改
4. 支持文件名搜索过滤，快速定位目标变更文件

## 二、内容更新与同步（更新内容）
1. 提交能力：面板顶部多行提交消息输入框，提供「提交暂存区」「提交所有更改」按钮，可选「提交后自动推送」
2. 远程同步：拉取Pull/推送Push/获取Fetch核心操作，实时显示远程分支领先/落后提交数
3. 冲突处理：识别合并冲突文件并标记，点击跳转编辑器冲突位置，提供快速解决入口
4. 支持撤销上一次提交，将更改回退至工作区

## 三、Git历史记录
1. 历史面板按时间倒序展示提交记录，显示短哈希、作者、时间、提交摘要
2. 点击提交展开变更文件列表，单文件可查看对应历史diff对比
3. 支持按作者、文件名、关键词筛选提交历史
4. 右键编辑器文件可查看「单文件专属修改历史」

## 四、配套体验（对标VS Code）
1. 底部状态栏：显示当前分支名、待提交/待拉取/待推送数量，点击快速唤起Git面板
2. 分支管理：点击状态栏分支名唤起列表，支持快速切换分支、创建新分支
3. 仓库初始化：未初始化Git的目录提供一键init入口
4. 兼容VS Code默认Git快捷键，支持自定义

## 五、技术约束
- 模块化扩展，不破坏原有编辑器核心功能
- Git命令层独立封装，错误场景全覆盖（网络/权限/冲突/仓库损坏）
- 万行文件diff渲染无卡顿，历史记录分页加载
- UI样式完全对齐现有编辑器设计规范
```
- [x] 2026-09-20：集成 Git 版本管理（更改管理 / 提交同步 / 历史记录 / 分支 / 状态栏 / 初始化）—— 见 CHANGELOG「2026-09-20 Git 版本管理：完整对标 VS Code 内置源代码管理」；新增 `services/git/`、`app/_git_controller.py`、`views/git_*.py`，137 项测试
- [ ] 2026-09-19：优化性能：降低内存占用、提高启动速度
- [x] 2026-09-19：标签行高度与大纲顶部高度保持一致，使整体视觉效果更协调，美观。
- [x] 2026-09-12：Ctrl+Shift+F，激活功能区的搜索、并聚焦到搜索框可直接输入以进行搜索
- [x] 2026-09-11：Ctrl+Shift+T：恢复打开历史关闭的文件
- [ ] 2026-09-08：增加对所有文本文档格式的支持
- [ ] 2026-09-08：支持多语言：中文、英文、繁体中文
- [x] 2026-09-08：优化大文件打开速度 —— 见 CHANGELOG「2026-09-19 性能：大文件打开提速」（解析快速构造器 + 行视图窗口化 + ListView.padding 留白）
- [x] 2026-09-08：编辑性能 —— 见 CHANGELOG「2026-09-19 性能」（单次重渲染 39.6ms → 4.5ms）
- [x] 2026-09-07：修复在引用语法中使用空格时，第一个空格宽度显示异常【Deepseek Harness 不能读图，输出代码让我跑后给他结果+他跑了让我给他效果说明约 7、8 回，laji】
- [x] 2026-09-08：优化日志功能，保证在异常（卡顿、报错、未响应等）时 Agent 能通过日志定位问题 —— 见 CHANGELOG「2026-09-19 可观测性」；日志 `logs/app.log`（`CS_MD_LOG_DIR` 可覆盖）、现场 `logs/crash-*.log`、级别 `settings.log_level` / `CS_MD_LOG_LEVEL`，排查方法见 AGENT.md 第 6 节
- [x] 2026-09-07：优化大纲内容显示，文字与编辑区标题颜色保持对应
- [x] 2026-09-05：去除界面上下左右（最左侧功能区左边、界面与顶部程序框架之间、大纲与最右侧、底部状态栏往下）的空白，保持界面的紧凑。整体要求遵循桌面端软件交互直觉，符合用户体验，界面美观、科学、清晰
通过按钮创建
在上方、下方添加行，左侧右侧添加列
选择整行、整列
删除整行、整列
鼠标拖拽选择区域
- [x] 2026-09-04：优化快捷键整体逻辑，严格控制焦点与快捷键的对应关系，避免在非编辑区时的影响到编辑区内容（比如搜索时 Ctrl+A 会异常全选编辑区内容，实际只需要选择搜索框的内容）
- [x] 2026-08-23：彻底重构界面，所有现有功能完整保留、不增不减。采用 VS Code 、Obsidian 风格横向四列式布局：第一列为功能栏：包括图标式的大功能选项（顶部文件、搜索，底部设置按钮），第二列为第一列对应的具体管理（比如文件、搜索等），第三列为文档编辑区，第四列为大纲。第二列与第四列支持一键收起。
- [x] 2026-08-23：检测到外部修改文件、重新加载时，切换到对应文件
- [x] 2026-08-22：减少项目依赖，去除 watchfiles（将 watchfiles 的功能调整为 watchdog，保持程序功能不变及稳定）
- [x] 2026-08-22：侧边栏的文件中，文件拖拽到其他文件夹需要拖拽到文件夹所在行（避免拖拽文件时抖动异常移动到别的文件夹）
- [x] 2026-08-22：修复 BUG：换行开启时，若触发换行，会异常丢失光标，无法继续编辑（需在换行时仍确保可继续编辑）
- [x] 2026-08-19：优化对文档首部元数据的支持：支持拖拽更换顺序，右键剪切、复制、粘贴、删除，上下高度紧凑，整体要求遵循桌面端软件交互直觉，符合用户体验，界面美观、科学、清晰，操作自然丝滑
  - 拖拽更换顺序：每行左侧拖拽把手（DRAG_INDICATOR，仅把手可拖起，避免与字段内文本选择冲突），
    整行为放置目标，拖拽悬停合法目标淡蓝高亮，松手后源行移到目标行位置（其余顺移）
  - 右键菜单：行右键剪切/复制/粘贴/删除（剪切=复制+删除；粘贴优先内部行缓冲，否则解析系统剪贴板
    "key: value"，无冒号整段作为值）；另有 ⋮ 按钮提供同菜单（不受字段原生菜单影响，始终可用）
  - 紧凑高度：行/表头/字段内边距收紧（vertical 3→1、表头 6→3、行 1→0）
  - 复制同时写系统剪贴板（跨应用可用）；粘贴插入当前行之后
- [ ] 2026-08-18：添加告警/提示块（Admonition）的支持
**告警 / 提示块（Admonition）**
- 统一 `> [!NOTE]` 等告警块语法
- 告警类型（note/warning/tip/error）标签命名规范
- [ ] 2026-08-18：打包后的 exe 程序支持本地 markdown 的打开方式以此应用打开
- [ ] 2026-08-18：配置 Shift+Alt+F 进行格式化，对 markdown 语法进行格式化：
1. 清理行尾多余空格，文件末尾统一保留一个换行
2. 行内代码统一用反引号包裹，反引号与内容间无多余空格
3. 任务列表 `- [ ]` / `- [x]` 语法与空格规范统一
4. 引用：统一 `>` 后加空格，多层嵌套引用的 `>` 数量与层级对应
5. 中英文混排加空格：中文与英文、数字、代码之间统一加半角空格
- [x] 2026-08-18：链接打开的文件，标签上需要为链接的名称
- [x] 2026-08-18：上、下、左、右键支持长按，光标持续移动
  - Flet 客户端 page 级 on_keyboard_event 只转发 KeyDownEvent（KeyRepeatEvent 被丢弃），
    长按方向键不会自动重复；KeyboardListener 控件转发 key_repeat（仅 key 无修饰键）
  - 左/右：客户端 key_repeat 驱动（同行移动不重建 TextField，焦点稳定）
  - 上/下：页面级 KeyDispatcher 自驱动定时器（真正的根因：cursor TextField 的
    ignore_up_down_keys 对上/下键返回 handled，Flutter 焦点链叶子优先分发，
    TextField 先吞掉上/下键 → 编辑器 KeyboardListener 永远收不到上/下键事件，
    定时器从 _on_key_down 启动的方案根本不会触发；KeyDispatcher 用 HardwareKeyboard
    全局处理器（焦点分发之前）能看到所有 KeyDown，KeyUp 不被拦截仍走
    KeyboardListener 到达 editor _on_key_up 停止）
  - 左/右：TextField 不拦截 → KeyboardListener key_repeat 驱动（无需定时器）
  - 边界检测读 nav_ref.current 的 actions.cursor_li（每帧重建写入最新值），
    而非闭包捕获的快照；容忍 3 tick 渲染滞后；松 Shift 等修饰键不误停
  - 守卫：浏览态/原文模式/原生控件（代码块/表格/公式）不启动自驱动
- [x] 2026-08-18：修复 BUG：修复对引用功能的编辑支持，当前存在行首使用>创建引用语法后光标消失、引用块无法点击编辑问题。要求遵循桌面端软件交互直觉，符合用户体验，界面美观、科学、清晰，操作自然丝滑
- [x] 2026-08-18：引用行内容为空时，按下回车键，行回退到常规段落（退出引用语法、保持光标）
  - on_submit 新增引用专用退出分支：before 按引用标记集剥离（strip("> ")）为空即退出，
    覆盖光标在行尾/前缀内/嵌套引用场景；有内容引用回车仍正常分割续行
- [x] 2026-08-18：优化光标功能性能，避免相关移动、重建占用资源过多，保持低负载、高性能，极速响应。
  - 同位置移动（点击同一位置）：零状态写入直接返回
  - 跨行移动：key=li 已触发重建，不再递增 nav_seq（省 1 次 state 更新）
  - 会话结束已重建（_end_input_session）时不重复脉冲
  - 同行移动脉冲节流：_CURSOR_PULSE_INTERVAL=250ms 窗口内只重建一次
    TextField（首个移动立即脉冲即时可见；长按/连击方向键不再逐键销毁重建）
  - 位置更新仍逐键渲染（left/top 实时），仅重建被节流；IME 路径不受影响
- [x] 2026-08-18：新建文件、新建文件夹时，需要重置弹窗里的输入框、并聚焦进行编辑（当前会保留上次的输入内容）
  - 弹窗 state 增加递增 instance，作为 FileActionDialog 的 key 触发组件重挂载
  - 输入框 use_state 重新以 input_value 初始化（新建为空）+ 重新 autofocus 聚焦编辑；
    重命名仍保留当前文件名作为默认值
- [x] 2026-08-16：每行的第一个字符会强制输入英文字符（不按实际的输入法来）：回退版本修复
- [x] 2026-08-18：引用功能编辑支持修复（行首 > 创建引用光标消失 / 引用块无法点击编辑）
  - handle_char_input 块级前缀结构变化（输入 > 创建引用等）→ 结束会话 + suppress_blur
    + nav_seq 重建重聚焦（修复 TextField 因渲染树形状变化重建后光标消失）
  - 引用前缀段 raw 改用源码实际字符（">" / "> " / ">> "），保持段/行长度一致
  - 前缀段边界（段末尾=内容起点）折叠：点击引用内容起点 caret 不再偏右一个前缀宽度
  - 最外层容器点击兜底：点击引用缩进/色条等 padding 死区定位到内容起点
  - 空引用（before 仅引用标记）按 Enter → 退出引用转常规段落并保持光标
    （旧逻辑 before="> " 的 strip() 得 ">" 非空，误走默认分割续行）
- [x] 2026-08-18：引用语法，整块背景变浅蓝（新增 quote_bg 亮/暗主题色，
  最外层引用容器挂浅蓝底，保留多级彩色左侧边框）
- [x] 2026-08-17：优化设置：
1. 右上角关闭按钮不随滚动条滚动
2. 整体更和谐、美观、精致
3. 快捷键分类：相对于默认快捷键有修改的进行标记
3. 增加关于：版本、联系邮箱、官方社群、用户手册
  - 关闭按钮固定：头部与分隔线移出滚动区，右列 = `[固定头部, 分隔线, 唯一滚动区]`；
    顺带修掉「外层列漏写 `expand=True` → 内层滚动列拿不到有界高度，内容被裁掉且滚轮完全无效」
    的真机 bug（详见 CHANGELOG 2026-09-19 条目）
  - 五个 tab 共用**同一**滚动区，不再有嵌套滚动条
  - 排版：语义分组卡片 + 统一定宽行工厂（各行左右边缘对齐）+ 角标；侧栏导航整行可点；
    全部撤掉 `ft.IconButton`（Material 固有高 40 压不下去）
  - 快捷键改动标记：相对 `ACTION_REGISTRY` 默认键比对（比对前 `normalize`，避免
    `ctrl+comma` / `ctrl+,` 等价写法误判），打「已修改 / 已清空」角标 + 分类汇总 + 「只看已修改」开关
  - 新增「关于」页（替换占位的「高级」tab）：版本 / 运行环境 / 作者 / 许可 + 联系邮箱 /
    官方社群 / 用户手册 + 相关链接，支持复制与打开
  - 新增 `config/app_meta.py` 作为应用身份与外链的**唯一来源**（关于页与帮助菜单共用）
- [x] 2026-08-18：光标移动时，保持持续可视，避免快速移动光标时光标从视线中丢失
  - 方案：保留闪烁，移动瞬间强制点亮——_set_cursor 递增 nav_seq（移动脉冲），
    cursor TextField key 变化 → 重建 + use_effect 重聚焦 → Flutter 光标以不透明相位
    重启闪烁（同行移动与跨行移动行为一致），静止后恢复正常闪烁
  - IME 安全：打字路径（handle_char_input / _move_cursor_inline）不经过 _set_cursor，
    不递增 nav_seq，组合态不受影响
  - 注：曾尝试叠加静态光标竖条方案，后按需求改为重建+重聚焦方案（已回退竖条）
- [x] 2026-08-21：增强自动保存功能：关闭程序、关闭文档、切换文档、光标从文档移出时等（详见 CHANGELOG 2026-08-21 条目）

- [ ] 2026-08-17：最后一行生成代码块时，无法往下一行编辑
- [x] 2026-08-18：去除 esc 切换侧边栏功能，仅使用 Ctrl+Shift+B 切换侧边栏
  - edit 层默认键从 escape 改为 ctrl+shift+b（browse 层本就是 ctrl+shift+b），两层统一
  - 移除 edit 分支的 Esc 处理；Esc 保留多光标清空 / 取消捕获 / 清除外向选区等原有用途
  - 同步更新本地 settings.json 中已持久化的 edit 层旧值
- [x] 2026-08-18：不同类型的文件，采用不同的图标（比如链接与.md 需要图标上区分）
  - 侧边栏文件树与最近文件列表均按扩展名区分图标（图片/代码/文档/压缩包/音视频等）
  - .lnk 一律用 SHORTCUT 链接图标，与 .md 的 DESCRIPTION 文档图标在形状上一眼可分；
    指向 .md 的快捷方式用 link 主题色 + 目标路径 tooltip，提示可在编辑器打开

## 早期
- [x] 修复 BUG：行尾双击时光标丢失（需要仍保有光标）
修复 BUG：使用本程序修改文件后，也会产生文件已被外部修改
后台实时保存，5 分钟一个存档
