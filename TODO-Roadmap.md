# ﻿Roadmap

>跨平台、替代Typora、VSCode（markdown部分）的文档编辑器/写作工具/知识管理工具/个人操作系统
- 特性：
  - 完全本地化：支持本地文件（类似typora编辑）、本地文件夹（类似obsidian仓库）
  - 所见即所得：基于Markdown语法
  - 多层级文件目录+标签体系管理
  - 实时保存，历史版本
  - 全局搜索


- 暂不考虑：
  - 双链
  - 标签体系：多级标签
  - 模板
  - 加密笔记
# TODO
- [x] 2026-08-23：彻底重构界面，所有现有功能完整保留、不增不减。采用 VS Code 、Obsidian风格横向四列式布局：第一列为功能栏：包括图标式的大功能选项（顶部文件、搜索，底部设置按钮），第二列为第一列对应的具体管理（比如文件、搜索等），第三列为文档编辑区，第四列为大纲。第二列与第四列支持一键收起。
- [ ] 2026-08-23：检测到外部修改文件、重新加载时，切换到对应文件
- [x] 2026-08-22：减少项目依赖，去除watchfiles（将watchfiles的功能调整为watchdog，保持程序功能不变及稳定）
- [x] 2026-08-22：侧边栏的文件中，文件拖拽到其他文件夹需要拖拽到文件夹所在行（避免拖拽文件时抖动异常移动到别的文件夹）
- [x] 2026-08-22：修复BUG：换行开启时，若触发换行，会异常丢失光标，无法继续编辑（需在换行时仍确保可继续编辑）
- [x] 2026-08-19：优化对文档首部元数据的支持：支持拖拽更换顺序，右键剪切、复制、粘贴、删除，上下高度紧凑，整体要求遵循桌面端软件交互直觉，符合用户体验，界面美观、科学、清晰，操作自然丝滑
  - 拖拽更换顺序：每行左侧拖拽把手（DRAG_INDICATOR，仅把手可拖起，避免与字段内文本选择冲突），
    整行为放置目标，拖拽悬停合法目标淡蓝高亮，松手后源行移到目标行位置（其余顺移）
  - 右键菜单：行右键剪切/复制/粘贴/删除（剪切=复制+删除；粘贴优先内部行缓冲，否则解析系统剪贴板
    "key: value"，无冒号整段作为值）；另有 ⋮ 按钮提供同菜单（不受字段原生菜单影响，始终可用）
  - 紧凑高度：行/表头/字段内边距收紧（vertical 3→1、表头 6→3、行 1→0）
  - 复制同时写系统剪贴板（跨应用可用）；粘贴插入当前行之后
- [ ] 2026-08-18：添加告警/提示块（Admonition）的支持
**告警 / 提示块（Admonition）**
- 统一 `> [!NOTE]` 等告警块语法
- 告警类型（note/warning/tip/error）标签命名规范
- [ ] 2026-08-18：按Ctrl+F时，侧边栏进入搜索、并聚焦搜索的输入框
- [ ] 2026-08-18：打包后的exe程序支持本地markdown的打开方式以此应用打开
- [ ] 2026-08-18：配置Shift+Alt+F进行格式化，对markdown语法进行格式化：
1. 清理行尾多余空格，文件末尾统一保留一个换行
2. 行内代码统一用反引号包裹，反引号与内容间无多余空格
3. 任务列表 `- [ ]` / `- [x]` 语法与空格规范统一
4. 引用：统一 `>` 后加空格，多层嵌套引用的 `>` 数量与层级对应
5. 中英文混排加空格：中文与英文、数字、代码之间统一加半角空格
- [ ] 2026-08-18：链接打开的文件，标签上需要为链接的名称
- [ ] 2026-08-18：
- [x] 2026-08-18：上、下、左、右键支持长按，光标持续移动
  - Flet 客户端 page 级 on_keyboard_event 只转发 KeyDownEvent（KeyRepeatEvent 被丢弃），
    长按方向键不会自动重复；KeyboardListener 控件转发 key_repeat（仅 key 无修饰键）
  - 左/右：客户端 key_repeat 驱动（同行移动不重建 TextField，焦点稳定）
  - 上/下：页面级 KeyDispatcher 自驱动定时器（真正的根因：cursor TextField 的
    ignore_up_down_keys 对上/下键返回 handled，Flutter 焦点链叶子优先分发，
    TextField 先吞掉上/下键 → 编辑器 KeyboardListener 永远收不到上/下键事件，
    定时器从 _on_key_down 启动的方案根本不会触发；KeyDispatcher 用 HardwareKeyboard
    全局处理器（焦点分发之前）能看到所有 KeyDown，KeyUp 不被拦截仍走
    KeyboardListener 到达 editor _on_key_up 停止）
  - 左/右：TextField 不拦截 → KeyboardListener key_repeat 驱动（无需定时器）
  - 边界检测读 nav_ref.current 的 actions.cursor_li（每帧重建写入最新值），
    而非闭包捕获的快照；容忍 3 tick 渲染滞后；松 Shift 等修饰键不误停
  - 守卫：浏览态/原文模式/原生控件（代码块/表格/公式）不启动自驱动
- [x] 2026-08-18：修复BUG：修复对引用功能的编辑支持，当前存在行首使用>创建引用语法后光标消失、引用块无法点击编辑问题。要求遵循桌面端软件交互直觉，符合用户体验，界面美观、科学、清晰，操作自然丝滑
- [x] 2026-08-18：引用行内容为空时，按下回车键，行回退到常规段落（退出引用语法、保持光标）
  - on_submit 新增引用专用退出分支：before 按引用标记集剥离（strip("> ")）为空即退出，
    覆盖光标在行尾/前缀内/嵌套引用场景；有内容引用回车仍正常分割续行
- [x] 2026-08-18：优化光标功能性能，避免相关移动、重建占用资源过多，保持低负载、高性能，极速响应。
  - 同位置移动（点击同一位置）：零状态写入直接返回
  - 跨行移动：key=li 已触发重建，不再递增 nav_seq（省 1 次 state 更新）
  - 会话结束已重建（_end_input_session）时不重复脉冲
  - 同行移动脉冲节流：_CURSOR_PULSE_INTERVAL=250ms 窗口内只重建一次
    TextField（首个移动立即脉冲即时可见；长按/连击方向键不再逐键销毁重建）
  - 位置更新仍逐键渲染（left/top 实时），仅重建被节流；IME 路径不受影响
- [x] 2026-08-18：新建文件、新建文件夹时，需要重置弹窗里的输入框、并聚焦进行编辑（当前会保留上次的输入内容）
  - 弹窗 state 增加递增 instance，作为 FileActionDialog 的 key 触发组件重挂载
  - 输入框 use_state 重新以 input_value 初始化（新建为空）+ 重新 autofocus 聚焦编辑；
    重命名仍保留当前文件名作为默认值
- [x] 2026-08-16：每行的第一个字符会强制输入英文字符（不按实际的输入法来）：回退版本修复
- [x] 2026-08-18：引用功能编辑支持修复（行首 > 创建引用光标消失 / 引用块无法点击编辑）
  - handle_char_input 块级前缀结构变化（输入 > 创建引用等）→ 结束会话 + suppress_blur
    + nav_seq 重建重聚焦（修复 TextField 因渲染树形状变化重建后光标消失）
  - 引用前缀段 raw 改用源码实际字符（">" / "> " / ">> "），保持段/行长度一致
  - 前缀段边界（段末尾=内容起点）折叠：点击引用内容起点 caret 不再偏右一个前缀宽度
  - 最外层容器点击兜底：点击引用缩进/色条等 padding 死区定位到内容起点
  - 空引用（before 仅引用标记）按 Enter → 退出引用转常规段落并保持光标
    （旧逻辑 before="> " 的 strip() 得 ">" 非空，误走默认分割续行）
- [x] 2026-08-18：引用语法，整块背景变浅蓝（新增 quote_bg 亮/暗主题色，
  最外层引用容器挂浅蓝底，保留多级彩色左侧边框）
- [ ] 2026-08-17：优化设置
- [x] 2026-08-18：光标移动时，保持持续可视，避免快速移动光标时光标从视线中丢失
  - 方案：保留闪烁，移动瞬间强制点亮——_set_cursor 递增 nav_seq（移动脉冲），
    cursor TextField key 变化 → 重建 + use_effect 重聚焦 → Flutter 光标以不透明相位
    重启闪烁（同行移动与跨行移动行为一致），静止后恢复正常闪烁
  - IME 安全：打字路径（handle_char_input / _move_cursor_inline）不经过 _set_cursor，
    不递增 nav_seq，组合态不受影响
  - 注：曾尝试叠加静态光标竖条方案，后按需求改为重建+重聚焦方案（已回退竖条）
- [x] 2026-08-21：增强自动保存功能：关闭程序、关闭文档、切换文档、光标从文档移出时等（详见 CHANGELOG 2026-08-21 条目）

- [ ] 2026-08-17：最后一行生成代码块时，无法往下一行编辑
- [x] 2026-08-18：去除esc切换侧边栏功能，仅使用Ctrl+Shift+B切换侧边栏
  - edit 层默认键从 escape 改为 ctrl+shift+b（browse 层本就是 ctrl+shift+b），两层统一
  - 移除 edit 分支的 Esc 处理；Esc 保留多光标清空 / 取消捕获 / 清除外向选区等原有用途
  - 同步更新本地 settings.json 中已持久化的 edit 层旧值
- [x] 2026-08-18：不同类型的文件，采用不同的图标（比如链接与.md需要图标上区分）
  - 侧边栏文件树与最近文件列表均按扩展名区分图标（图片/代码/文档/压缩包/音视频等）
  - .lnk 一律用 SHORTCUT 链接图标，与 .md 的 DESCRIPTION 文档图标在形状上一眼可分；
    指向 .md 的快捷方式用 link 主题色 + 目标路径 tooltip，提示可在编辑器打开

## 早期
- [x] 修复BUG：行尾双击时光标丢失（需要仍保有光标）
修复BUG：使用本程序修改文件后，也会产生文件已被外部修改
后台实时保存，5分钟一个存档












